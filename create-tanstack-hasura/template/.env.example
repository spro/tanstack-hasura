POSTGRES_DB=microblog
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

HASURA_PORT=8080
HASURA_GRAPHQL_ADMIN_SECRET=dev-admin-secret
HASURA_GRAPHQL_UNAUTHORIZED_ROLE=anonymous

# Client-side envs for the app if you want to call Hasura directly from the browser.
# Prefer server-side access for privileged operations.
VITE_HASURA_URL=http://localhost:8080/v1/graphql
VITE_HASURA_ADMIN_SECRET=dev-admin-secret
